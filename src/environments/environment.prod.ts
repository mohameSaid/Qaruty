export const environment = {
  production: true,
  baseUrl: 'https://qaryati.onrender.com',
  /** See the dev environment.ts for the important caveat about this being fake, client-side-only auth. */
  auth: {
    username: 'admin',
    password: 'admin123',
  },
};
